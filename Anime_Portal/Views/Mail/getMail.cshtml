@model Bll.DTO.MailHeaderData

<div class="modal-body">
    <div class="form-group">
        <h4>Inbox</h4>
        <label>From: @Model.From</label><br />
        <label>Subject: @Model.Title</label>
        </div>
     <div class="col-lg-12">@Html.Raw(Model.Text)</div>

</div>